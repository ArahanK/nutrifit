package com.hotel.Nutrition;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;

@RestController
class NutritionController {
    private final NutritionRepository repo;

    NutritionController(NutritionRepository repo){
        this.repo = repo;
    }

    @GetMapping("/TestNutrition")
    public String hello(){
        return "Hello Nutrition World";
    }

    @GetMapping("/GetNutrition")
    public Nutrition getFoodInfoByName(@RequestParam String name){
        // Logic to retrieve food information from the local database
        // ...

        // Return the food information
        return new Nutrition("testFoodName", "protein: "1, "carbs: "1,"fat: "1);
    }

    @PostMapping("/AddNutrition")
    public void addNutrition(){
        // Logic to add food macro information to the local userlog database
        return;
    }
    
}